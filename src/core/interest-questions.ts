// This file has been superseded by the AI-driven button system.
// The AI now generates question + choices + cluster values in one call.
// Pending choices are stored in the profile (_pendingChoices) so button
// clicks resolve without a second LLM call.
